class_name: ApiTester
modules:
    enabled: [ApiHelper, DbHelper, Laravel5, REST]
    config:
      REST:
           depends: PhpBrowser
           url: 'http://api.tenfour.local/'
      DbHelper:
           dsn: 'mysql:host=localhost;dbname=rollcall'
           user: 'root'
           password: 'secret'
           dump: 'tests/_data/dump.sql'
env:
  testing:
    modules:
      config:
        REST:
             url: 'http://api.tenfour.local/'
        DbHelper:
          dsn: 'mysql:host=mysql;dbname=rollcall'
          user: rollcall
          password: rollcall
